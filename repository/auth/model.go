package auth

type IAuthRepository interface {
	// LoginByEmail()
	// ForgetPassword
	// ResetPassword
}

// func (r *AuthRepository) LoginByEmail(user *models.User) error {
// 	err := r.PostgreDB.Where("email", user.Email).Where("password")
// }
